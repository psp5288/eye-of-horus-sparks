{
  "name": "eye-of-horus-sparks",
  "version": "1.0.0",
  "description": "AI-powered crowd intelligence for live events",
  "scripts": {
    "dev": "python -m http.server 3000 --directory frontend",
    "build": "echo 'Static frontend — no build step required'",
    "start": "python -m http.server 3000 --directory frontend",
    "test": "echo 'Frontend tests not configured'",
    "backend": "uvicorn backend.main:app --reload --port 8000",
    "all": "concurrently \"npm run backend\" \"npm run dev\""
  },
  "dependencies": {
    "axios": "^1.6.0",
    "chart.js": "^4.4.0",
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "keywords": [
    "crowd-safety",
    "live-events",
    "ai",
    "claude",
    "swarm-simulation",
    "hackathon"
  ],
  "author": "Eye of Horus Team",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "python": ">=3.11.0"
  }
}
